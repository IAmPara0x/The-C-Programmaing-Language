#include <stdio.h>

/*
Write the function itob(n,s,b) that converts the integer n into a base b
haracter representation in the string s. In particular, itob(n,s,16) formats s as a
hexadecimal integer in s.
 */


/* void itob (int n, char s[], b) */
/* { */
/* } */

int main()
{

  unsigned char x = 1;
  unsigned char x1 = 64;
  printf("%d\n", (x << 2) ^ x1);
  return 0;
}
